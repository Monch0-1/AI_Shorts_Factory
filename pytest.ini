[pytest]
markers =
    gemini_test: marks tests that call the Gemini API (skipped by default to save tokens)
    elevenlabs_test: marks tests that call the ElevenLabs API (skipped by default, set RUN_ELEVENLABS_TESTS=true to run)
